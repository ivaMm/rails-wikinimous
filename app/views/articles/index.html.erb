<div class="container">
  <div class="container-articles">
    <% @articles.each do |article| %>
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%= article.content[0..30] %>...</p>
    <% end %>
  </div>
</div>